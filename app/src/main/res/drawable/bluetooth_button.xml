<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/black" /> <!-- 버튼 배경색 -->
    <corners android:radius="16dp" /> <!-- 모서리 반경 (버튼의 둥글기) -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- 테두리 색상 및 두께 (선택사항) -->
</shape>